select mems.surname, mems.firstname, mems.memid, min(books.starttime) as starttime
		from cd.bookings books
		inner join cd.members mems
		on mems.memid = books.memid
		where
			starttime >= '2012-09-01'
		group by mems.surname, mems.firstname, mems.memid
order by memid